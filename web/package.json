{
  "name": "webwheel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "list": "node scripts/list.mjs",
    "sync": "node scripts/sync.mjs",
    "deploy": "node scripts/sync.mjs --bundle && cd ../worker && npx wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
